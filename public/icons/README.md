# Integration Icons

Drop SVG logos for integrations here if you'd like to replace the built-in letter glyphs used by the marquee strip.

The marquee component (`components/ui/MarqueeStrip.tsx`) renders simple letter-mark glyphs by default so the page looks complete without any extra assets.
